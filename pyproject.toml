[project]
name = "spotify-usage"
version = "0.1.0"
description = "Analysis on Spotify Extended Streaming History data"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyterlab>=4.5.8",
    "python-dotenv>=1.2.2",
    "quoi",
]

[dependency-groups]
dashboard = [
    "dash>=4.4.0",
    "dash-bootstrap-components>=2.0.4",
]
dev = [
    "ruff>=0.15.17",
]

[tool.ruff]
line-length = 120

[tool.uv.sources]
quoi = { git = "https://github.com/jojustleft/quoi" }
